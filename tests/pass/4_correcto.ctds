program
{
  integer whiletrue = 1;
  void eq_assert(integer expected, integer result) extern;
  void main()
  {
    bool ifelse = true;
    if (ifelse) then
    {
      return;
    }
    else
    {
      integer x = 2;
      integer y = 3;

      integer elsethen = x + y;
      eq_assert(5, elsethen);
      return;
    }
  }
}
