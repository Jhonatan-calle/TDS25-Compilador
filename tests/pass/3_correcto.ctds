program
{
    integer x = 2;
    integer inc(integer z)
    {
        return z + 1;
    }
    integer get_int() extern;
    void printInt(integer i) extern;
    integer eq_assert(integer expected, integer result) extern;
    void main()
    {
        integer y = 0;
        y = get_int();
        if (y == 1) then
        {
            printInt(y);
        }
        else
        {
            integer r = eq_assert(42, y);
            printInt(inc(y));
            y = inc(y);
            r = eq_assert(43, y);
            return r;
        }
    }
}
