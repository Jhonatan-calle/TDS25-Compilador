program
{
  bool foo(integer bar)
  {
    return true;
  }
  void eq_assert(integer expected, integer result) extern;
  void main()
  {
    integer bar = 5;
    bool b = foo(bar);
    eq_assert(5, bar);
    return;
  }
}
